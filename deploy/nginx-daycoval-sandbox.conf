# Daycoval sandbox — instalação: deploy/README.md (comandos shell NÃO entram neste arquivo)
# Ajuste server_name para o IP público da VPS.

server {
    listen 80;
    listen [::]:80;
    server_name 31.97.165.27;

    location / {
        proxy_pass https://apigwsandbox.daycoval.com.br;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_set_header Host apigwsandbox.daycoval.com.br;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
